import { PriceAfterDisPipe } from './price-after-dis.pipe';

describe('PriceAfterDisPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceAfterDisPipe();
    expect(pipe).toBeTruthy();
  });
});
